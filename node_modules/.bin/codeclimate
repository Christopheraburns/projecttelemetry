#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../codeclimate-test-reporter/bin/codeclimate.js" "$@"
  ret=$?
else 
  node  "$basedir/../codeclimate-test-reporter/bin/codeclimate.js" "$@"
  ret=$?
fi
exit $ret
